ifdef::manvolnum[]
pmgtunnel(8)
============
:pmg-toplevel:

NAME
----

pmgtunnel - Proxmox Mail Gateway Cluster Tunnel Daemon


SYNOPSIS
--------

include::generated/pmgtunnel.8-synopsis.adoc[]

DESCRIPTION
-----------
endif::manvolnum[]

ifndef::manvolnum[]
pmgtunnel - Cluster Tunnel Daemon
=================================
endif::manvolnum[]

This daemon creates an ssh tunnel to the Postgres databases on other
cluster nodes (port 5432). The tunnel is used to synchronize the
database, using an application-specific, asynchronous replication
algorithm.

ifdef::manvolnum[]
include::pmg-copyright.adoc[]
endif::manvolnum[]
